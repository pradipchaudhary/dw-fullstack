const numbers = [1, 2, 3, 4];
// console.log(numbers);

const fruits = Array("Banana", "Apple", "Orange", "Mango", "other");

for (let index = 0; index < 5; index++) {
    console.log("for loop:", index);
}

let whileindex = 0;
while (whileindex < 9) {
    console.log("White index:- ", whileindex);
    whileindex++;
}

// do while loop
let doindex = 8;
do {
    console.log("do white index: ", doindex);
    doindex++;
} while (doindex > 88);
